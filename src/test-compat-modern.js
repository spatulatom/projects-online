// This file tests browser compatibility\nconst asyncFunction = async () => {\n  const result = await Promise.resolve('modern feature');\n  console.log(result);\n};\n\nconst obj = { ...{ a: 1 }, ...{ b: 2 } }; // Spread operator\nconst arr = [...[1, 2], ...[3, 4]]; // Array spread\n\n// Optional chaining\nconst val = obj?.a?.b?.c;\n\n// Nullish coalescing\nconst fallback = val ?? 'default';\n\nexport { asyncFunction, obj, arr };